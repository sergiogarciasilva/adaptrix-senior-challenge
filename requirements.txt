# Core dependencies
PyMuPDF==1.24.0
pydantic>=2.0.0

# Fuzzy matching (if using Option C)
rapidfuzz>=3.0.0

# LLM clients (for advanced implementations)
openai>=1.0.0
anthropic>=0.18.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.0
pytest-benchmark==4.0.0

# Type checking
mypy>=1.0.0
